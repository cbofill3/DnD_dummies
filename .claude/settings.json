{
  "$comment": "Tracked project settings. Local-only overrides go in settings.local.json, which is gitignored. Allowlist covers the read-only lookups that run constantly, so book searches don't interrupt a session with permission prompts.",
  "permissions": {
    "allow": [
      "Bash(python .claude/tools/booksearch.py:*)",
      "Bash(python .claude/tools/mmindex.py:*)",
      "Bash(pdftoppm:*)",
      "Bash(command -v:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)"
    ]
  }
}
